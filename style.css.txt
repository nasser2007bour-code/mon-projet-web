/* ========================================
   RESET - إعادة تعيين الإعدادات الافتراضية
   ======================================== */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

/* ========================================
   BODY - إعدادات عامة للصفحة
   ======================================== */
body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background-color: #faf7f2;
    color: #3e2723;
    line-height: 1.6;
}

/* ========================================
   HEADER & NAVIGATION - شريط التنقل
   ======================================== */
header {
    background-color: #3e2723;
    color: white;
    padding: 15px 30px;
    position: sticky;
    top: 0;
    z-index: 100;
}

nav {
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
}

.logo h1 {
    font-size: 1.5rem;
}

.nav-links {
    display: flex;
    list-style: none;
    gap: 25px;
}

.nav-links li a {
    color: white;
    text-decoration: none;
    font-weight: bold;
    padding: 8px 12px;
    transition: 0.3s;
    border-radius: 5px;
}

.nav-links li a:hover {
    background-color: #6d4c41;
}

/* زر القائمة للجوال */
.burger {
    display: none;
    flex-direction: column;
    cursor: pointer;
}

.burger span {
    width: 25px;
    height: 3px;
    background-color: white;
    margin: 3px 0;
    transition: 0.3s;
}

/* ========================================
   HERO SECTION - القسم الترحيبي
   ======================================== */
.hero {
    background: linear-gradient(rgba(0,0,0,0.5), rgba(0,0,0,0.5)), url('../images/hero.jpg');
    background-size: cover;
    background-position: center;
    height: 500px;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    color: white;
}

/* بديل إذا لم توجد الصورة */
.hero {
    background-color: #5d4037;
    background-blend-mode: overlay;
}

.hero-content h1 {
    font-size: 3rem;
    margin-bottom: 20px;
}

.hero-content p {
    font-size: 1.2rem;
    margin-bottom: 30px;
}

/* ========================================
   BUTTONS - الأزرار
   ======================================== */
button {
    background-color: #d2691e;
    color: white;
    border: none;
    padding: 12px 25px;
    font-size: 1rem;
    cursor: pointer;
    border-radius: 30px;
    transition: 0.3s;
}

button:hover {
    background-color: #a0522d;
    transform: scale(1.05);
}

/* ========================================
   FEATURES - قسم المميزات
   ======================================== */
.features {
    display: flex;
    justify-content: space-around;
    flex-wrap: wrap;
    padding: 50px 20px;
    text-align: center;
    gap: 30px;
}

.feature {
    background-color: white;
    padding: 30px;
    border-radius: 15px;
    box-shadow: 0 5px 15px rgba(0,0,0,0.1);
    width: 250px;
    transition: 0.3s;
}

.feature:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 25px rgba(0,0,0,0.15);
}

/* ========================================
   ABOUT PAGE - صفحة من نحن
   ======================================== */
.about-section, .contact-section, .menu-section, .gallery-section {
    padding: 50px 20px;
    max-width: 1200px;
    margin: auto;
}

.about-section h1, .menu-section h1, .gallery-section h1, .contact-section h1 {
    text-align: center;
    margin-bottom: 40px;
    font-size: 2rem;
    color: #3e2723;
}

.about-content {
    display: flex;
    gap: 40px;
    flex-wrap: wrap;
    margin-top: 30px;
}

.about-content img {
    width: 100%;
    max-width: 400px;
    border-radius: 15px;
    object-fit: cover;
}

.about-content div {
    flex: 1;
}

/* ========================================
   MENU PAGE - صفحة القائمة
   ======================================== */
.menu-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 30px;
    margin-top: 40px;
}

.menu-item {
    background-color: white;
    border-radius: 15px;
    padding: 20px;
    text-align: center;
    box-shadow: 0 5px 15px rgba(0,0,0,0.1);
    transition: 0.3s;
}

.menu-item:hover {
    transform: translateY(-5px);
}

.menu-item img {
    width: 100%;
    height: 200px;
    object-fit: cover;
    border-radius: 10px;
}

.price {
    display: block;
    font-size: 1.3rem;
    font-weight: bold;
    color: #d2691e;
    margin: 10px 0;
}

/* ========================================
   GALLERY PAGE - صفحة معرض الصور
   ======================================== */
.gallery-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 20px;
    margin-top: 40px;
}

.gallery-img {
    width: 100%;
    height: 250px;
    object-fit: cover;
    border-radius: 10px;
    cursor: pointer;
    transition: 0.3s;
}

.gallery-img:hover {
    transform: scale(1.02);
    box-shadow: 0 5px 20px rgba(0,0,0,0.2);
}

/* ========================================
   CONTACT PAGE - صفحة الاتصال
   ======================================== */
.contact-container {
    max-width: 600px;
    margin: auto;
}

#contactForm {
    display: flex;
    flex-direction: column;
    gap: 20px;
    margin-top: 30px;
}

.form-group {
    display: flex;
    flex-direction: column;
    gap: 8px;
}

.form-group label {
    font-weight: bold;
    color: #3e2723;
}

#contactForm input, #contactForm textarea {
    padding: 12px;
    border: 1px solid #ddd;
    border-radius: 8px;
    font-family: inherit;
    font-size: 1rem;
}

#contactForm input:focus, #contactForm textarea:focus {
    outline: none;
    border-color: #d2691e;
    box-shadow: 0 0 5px rgba(210,105,30,0.3);
}

/* ========================================
   FOOTER - تذييل الصفحة
   ======================================== */
footer {
    background-color: #3e2723;
    color: white;
    text-align: center;
    padding: 20px;
    margin-top: 50px;
}

/* ========================================
   RESPONSIVE DESIGN - جعل الموقع متجاوباً
   ======================================== */
@media (max-width: 768px) {
    .nav-links {
        display: none;
        flex-direction: column;
        width: 100%;
        text-align: center;
        background-color: #3e2723;
        margin-top: 15px;
        padding: 15px 0;
    }

    .nav-links.active {
        display: flex;
    }

    .burger {
        display: flex;
    }

    .hero-content h1 {
        font-size: 2rem;
    }

    .about-content {
        flex-direction: column;
        align-items: center;
    }

    .features {
        padding: 30px 15px;
    }

    .feature {
        width: 100%;
        max-width: 300px;
    }
}

@media (max-width: 480px) {
    .hero {
        height: 350px;
    }

    .hero-content h1 {
        font-size: 1.5rem;
    }

    .menu-grid, .gallery-grid {
        grid-template-columns: 1fr;
    }
}